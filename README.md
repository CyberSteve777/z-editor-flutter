# z_editor

Level Editor for Plants vs. Zombies 2 Chinese

## Initial setup
Prerequisites: `Android Studio, Flutter SDK, Other sdks that first two might ask`

After getting the project locally, you might need to disable web support. To do that, run in project root this command:
```bash
flutter config --no-enable-web
```
